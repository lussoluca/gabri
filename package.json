{
  "name": "gabri",
  "private": true,
  "type": "module",
  "workspaces": [
    "frontend",
    "backend",
    "editor"
  ],
  "scripts": {
    "content": "node scripts/build-content.mjs",
    "content:watch": "node --watch-path=content scripts/build-content.mjs",
    "dev:frontend": "npm run content && npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "dev:editor": "npm run dev --workspace editor",
    "build": "npm run content && npm run build --workspace frontend && npm run build --workspace backend && npm run build --workspace editor"
  },
  "devDependencies": {
    "js-yaml": "^4.1.0"
  }
}
